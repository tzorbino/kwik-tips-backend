package com.zony.kwiktips.repository;

import com.zony.kwiktips.model.TipReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipReportRepository extends JpaRepository<TipReport, Long> {
}
